# Backend Environment Variables

# API Settings
DEBUG=False
LOG_LEVEL=INFO
API_HOST=0.0.0.0
API_PORT=8000

# Security - CRITICAL: Generate secure random keys (see instructions at bottom)
SECRET_KEY=<GENERATE_SECURE_64_CHAR_KEY>
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Admin
ADMIN_EMAIL=admin@face-seek.com
ADMIN_API_KEY=<GENERATE_SECURE_64_CHAR_KEY>

# Database - Use PostgreSQL in production (NOT SQLite)
DATABASE_URL=postgresql+psycopg2://faceseek:<POSTGRES_PASSWORD>@postgres:5432/faceseek
# WARNING: SQLite (sqlite:///./faceseek.db) is NOT recommended for production!

# Redis (optional)
# REDIS_URL=redis://localhost:6379/0

# CORS - Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://face-seek.com,https://www.face-seek.com

# Upload Settings
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE=10485760

# Public Base URL
PUBLIC_BASE_URL=https://www.face-seek.com

# API Keys (optional)
# BING_API_KEY=
# YANDEX_API_KEY=
# YANDEX_FOLDER_ID=
# FACECHECK_API_KEY=
# SERPAPI_API_KEY=
# RAPIDAPI_KEY=
# OPENAI_API_KEY=

# Email (SMTP) - optional
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@faceseek.io

# LemonSqueezy (optional)
# LEMONSQUEEZY_API_KEY=
# LEMONSQUEEZY_STORE_ID=
# LEMONSQUEEZY_WEBHOOK_SECRET=

# =============================================================================
# HOW TO GENERATE SECURE KEYS (PowerShell on Windows):
# =============================================================================
# Run this in PowerShell to generate secure random keys:
#
# $secretKey = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
# $adminKey = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
#
# Write-Host "SECRET_KEY=$secretKey"
# Write-Host "ADMIN_API_KEY=$adminKey"
#
# Then copy the generated values into this .env file
# =============================================================================
#
# IMPORTANT PRODUCTION CHECKLIST:
# - Set DEBUG=False (NEVER use DEBUG=True in production!)
# - Use PostgreSQL database (NOT SQLite)
# - Generate strong SECRET_KEY and ADMIN_API_KEY
# - Configure SMTP for email functionality
# - Update CORS_ORIGINS with your actual domain
# - NEVER commit .env file with real credentials to git!
