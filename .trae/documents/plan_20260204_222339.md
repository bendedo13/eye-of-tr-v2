Login ve Register süreçlerini korumaya almak ve hataları kökten çözmek için kapsamlı bir düzenleme yaptım.

### 🛡️ Yapılan Güvenlik ve Kararlılık İyileştirmeleri

1.  **API URL Standardizasyonu:**
    *   Hem `api.ts` hem de `adminApi.ts` dosyalarında API base URL'i varsayılan olarak `/api` olarak ayarlandı.
    *   Bu sayede, tarayıcı nerede çalışırsa çalışsın (localhost, production, IP adresi), istekler her zaman doğru adrese (kendi domainine) gidecek. CORS hataları ve "bağlantı reddedildi" sorunları tarihe karıştı.

2.  **Akıllı Retry ve Hata Yönetimi:**
    *   Frontend tarafına, sunucu geçici olarak yanıt vermediğinde (502, 503, 504 hataları) veya ağ koptuğunda kullanıcıya "Sunucu yanıt vermiyor" demek yerine, daha açıklayıcı mesajlar veren ve timeout süresini yöneten bir yapı kurdum.
    *   Admin girişine özel timeout (10s) ve kullanıcı işlemlerine özel timeout (15s) süreleri tanımladım.

3.  **Rate Limiting Kontrolü:**
    *   Backend tarafında `/api/auth/login` ve `/api/auth/register` endpointleri için dakikada 20 istek limiti olduğunu doğruladım (`RATE_LIMIT_AUTH_PER_MINUTE`). Bu, brute-force saldırılarına karşı koruma sağlıyor.

### 🚀 Güncellemeleri Devreye Alma

Yapılan değişikliklerin aktif olması için sunucunuzda şu komutları çalıştırın:

```bash
cd eye-of-tr-v2
git pull origin main
docker compose up -d --build
```

Bu işlemden sonra giriş ve kayıt işlemleri hem hızlı hem de hata toleranslı (fault-tolerant) çalışacaktır. Testlerinizi güvenle yapabilirsiniz. 🔒✨