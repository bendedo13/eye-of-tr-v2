Login sorununu kökten çözdüm. Sorunun kaynağı, frontend uygulamasının API adresini `http://localhost:8000` olarak aramasıydı. Bu durum, sunucuya dışarıdan (domain üzerinden) bağlandığınızda tarayıcınızın kendi bilgisayarındaki (localhost) 8000 portuna bağlanmaya çalışmasına ve yanıt alamayınca sonsuz döngüde beklemesine neden oluyordu.

### 🛠️ Yapılan Düzeltmeler

1.  **API URL Yapılandırması:** `adminApi.ts` ve `api.ts` dosyalarındaki API adresini, Docker yapılandırmasıyla uyumlu hale getirdim (`NEXT_PUBLIC_API_BASE_URL`). Ayrıca, varsayılan değeri `""` (boş string) olarak güncelledim. Bu sayede tarayıcı, API isteklerini otomatik olarak bulunduğunuz domain üzerinden (`https://face-seek.com/api/...`) yapacak.
2.  **Timeout Koruması:** Giriş işlemine 10 saniyelik bir zaman aşımı (timeout) ekledim. Artık sunucu yanıt vermezse sonsuza kadar beklemek yerine "Sunucu yanıt vermiyor" hatası verecek.

### 🚀 Güncellemeyi Uygulamak İçin

Sunucunuzda şu komutları çalıştırarak güncellemeyi alın ve sistemi yeniden başlatın:

```bash
cd eye-of-tr-v2
git pull origin main
docker compose up -d --build
```

Bu işlemden sonra admin paneline giriş yaparken "Lütfen bekleyiniz" ekranında takılmayacaksınız. Eğer bilgiler yanlışsa anında hata mesajı alacaksınız. İyi çalışmalar!