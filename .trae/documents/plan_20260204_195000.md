Tüm güvenlik ve konfigürasyon eksikliklerini giderdim ve projeyi profesyonel bir dağıtım (deploy) sürecine hazırladım.

### 🛠️ Yapılan Değişiklikler

1.  **Güvenlik ve Şifre Yönetimi**
    *   `docker-compose.yml` dosyasındaki **tüm hardcoded şifreler (faceseek/faceseek) kaldırıldı**.
    *   Artık tüm kritik veriler (`POSTGRES_PASSWORD`, `SECRET_KEY`, `ADMIN_API_KEY`) `.env` dosyasından okunuyor.

2.  **Nginx Güvenlik İyileştirmesi**
    *   `deploy/nginx/faceseek.conf` dosyasına **Security Headers** (XSS koruması, Sniffing engelleme vb.) eklendi.
    *   SSL sertifikası (Certbot/Let's Encrypt) kurulumu için `.well-known` bloğu hazırlandı.
    *   Domain adı değişken olarak ayarlandı.

3.  **🚀 Otomatik Kurulum Sihirbazı (YENİ)**
    *   Manuel `.env` düzenleme hatasını önlemek için `scripts/generate_production_env.py` scripti hazırladım.
    *   Bu script:
        *   Sizden domain adını ister.
        *   Kriptografik olarak **güvenli, rastgele şifreler** üretir.
        *   Projenin çalışması için gereken `.env` dosyasını otomatik oluşturur.
        *   Domaininize özel Nginx konfigürasyon dosyasını hazırlar.

### 🏁 Nasıl Yayına Alacaksınız?

Sunucunuzda (Ubuntu 22.04) proje klasörüne girdikten sonra sadece şu komutu çalıştırın:

```bash
python3 scripts/generate_production_env.py
```

Sihirbaz sizi yönlendirecek ve kurulumu tamamlayacaktır. Ardından `docker-compose up -d` ile sistemi güvenli bir şekilde başlatabilirsiniz.