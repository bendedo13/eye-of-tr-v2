Değişiklikleri GitHub'a push ettim ve canlıda test etmeniz için gerekli bilgileri aşağıda derledim.

### 🚀 Deployment ve Admin Girişi

**1. Kodları VPS'ye Çekin:**
Sunucunuza bağlanıp kodları güncelleyin ve yeniden başlatın:
```bash
cd eye-of-tr-v2
git pull origin main
docker compose up -d --build
```

**2. Admin Giriş Bilgileri:**
Admin paneli (`/admin/login`) için özel bir kullanıcı adı yoktur; sistem **API Anahtarı (Key)** tabanlı çalışır.

*   **E-Posta:** Herhangi bir e-posta girebilirsiniz (Örn: `admin@faceseek.io`).
*   **Erişim Anahtarı (Şifre):** Bu anahtar, sunucunuzdaki `.env` dosyasında `ADMIN_API_KEY` olarak kayıtlıdır.

🔑 **Şifrenizi (API Key) Öğrenmek İçin:**
Sunucunuzda şu komutu çalıştırın:
```bash
grep ADMIN_API_KEY .env
```
*(Çıkan `ADMIN_API_KEY=...` kısmındaki uzun şifreyi kopyalayıp giriş ekranındaki "Erişim Anahtarı" alanına yapıştırın.)*

### 🧪 Test Edebileceğiniz Yeni Özellikler

1.  **Unlimited Veri Merkezi:** Admin panelinden kendinize "Unlimited" paketi tanımlayın, ardından `/data-platform` sayfasına gidip bir web sitesi taratın.
2.  **Akıllı Bildirimler:** Admin panelinden "Bildirimler" sekmesine gidin ve test bildirimi gönderin. Sağ üstte sesli ve süreli (2sn zorunlu) uyarıyı göreceksiniz.
3.  **OpenAI Analizi:** Bir arama yapın. Eğer sonuç bulunursa yapay zeka özetini, bulunamazsa (örn: siyah ekran yükleyerek) "Görüntü kalitesi yetersiz" veya "Gizlilik" uyarısını test edebilirsiniz.

İyi testler! 🚀