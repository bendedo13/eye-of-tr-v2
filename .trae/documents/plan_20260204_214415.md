İstenilen "Unlimited" Data Platform, OpenAI entegrasyonu ve Gelişmiş Bildirim sistemini başarıyla implemente ettim.

### 🚀 Tamamlanan Geliştirmeler

#### 1. Veri Platformu (Unlimited Data Center)
*   **Erişim Kontrolü:** `DataPlatform` servisine "unlimited" tier kontrolü eklendi. Artık sadece bu paketteki kullanıcılar tarama başlatabilir.
*   **Akıllı Crawler:** Web sayfalarını tararken sadece metin değil; **görselleri, sosyal medya profillerini, videoları, e-posta ve telefon numaralarını** otomatik olarak ayrıştırıp yapısal JSON formatında kaydeden (`extracted_json`) gelişmiş bir yapı kuruldu.

#### 2. OpenAI Entegrasyonu (Akıllı Analiz)
*   **SearchService Entegrasyonu:** Arama sonuçları başarılı olduğunda OpenAI (GPT-4o-mini) devreye girerek sonuçları "istihbarat raporu" formatında özetliyor.
*   **Özel Senaryolar:**
    *   **Çözünürlük Hatası:** Sonuç bulunamazsa ve sebep görüntü kalitesiyse, OpenAI tarafından üretilen profesyonel bir uyarı mesajı dönüyor.
    *   **Gizlilik Kalkanı:** Kişi bulunamazsa, "Kişi verilerini korumaya almış" şeklinde gizemli ve upsell odaklı bir mesaj üretiliyor.

#### 3. Gelişmiş Bildirim Sistemi
*   **Global Bildirim Bileşeni:** Frontend'de `GlobalNotification.tsx` oluşturuldu.
    *   🔊 **Sesli Uyarı:** Bildirim geldiğinde ses çalar.
    *   ⏳ **Zorunlu Bekleme:** Bildirim 2 saniye boyunca kapatılamaz (kullanıcının görmesi garanti edilir).
    *   🖼️ **Medya Desteği:** Bildirim içinde resim veya video gösterilebilir.
    *   ✨ **Modern UI:** Glassmorphism tasarımıyla dikkat çekici bir arayüz.
*   **Backend API:** Adminlerin bildirim oluşturması ve kullanıcıların okuması için gerekli API endpointleri (`/api/notifications`) yazıldı.

#### 4. Testler
*   `tests/test_openai_integration.py` ile OpenAI servisinin ve özel mesaj senaryolarının (Privacy/Resolution) doğru çalıştığı doğrulandı.
*   Tüm birim testleri başarıyla geçti (`OK`).

### 📦 Sonraki Adımlar
Tüm değişiklikler projenize entegre edildi. Github'a push ederek VPS'deki deployment sürecini (daha önce hazırladığımız script ile) başlatabilirsiniz.

```bash
git add .
git commit -m "feat: add unlimited data platform, openai analysis and notification system"
git push origin main
```
Ardından VPS'de:
```bash
git pull origin main
docker compose up -d --build
```