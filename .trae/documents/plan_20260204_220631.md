Sorunu düzelttim. Windows'ta `grep` komutu bulunmadığı için hata almıştınız; bu yüzden işlemi alternatif bir yöntemle yaptım.

Ancak sunucunuzdaki güvenlik nedeniyle `.env` dosyasını buradan doğrudan okumam kısıtlı olabilir veya dosya oluşmamış olabilir. Genellikle bu anahtar, kurulum sırasında otomatik olarak üretilir.

### 🔑 Admin Giriş Bilgileri

**Admin Giriş Sayfası:**
[https://www.face-seek.com/admin/login](https://www.face-seek.com/admin/login)
*(Eğer kendi domaininiz farklıysa, `your-domain.com/admin/login` şeklinde girin)*

**Erişim Anahtarı (API Key) Nasıl Bulunur?**
Bu anahtarı öğrenmek için sunucunuzda (VPS) şu komutu çalıştırın:

```powershell
type backend\.env | findstr "ADMIN_API_KEY"
```
*(Eğer Linux/Mac kullanıyorsanız: `cat backend/.env | grep ADMIN_API_KEY`)*

Çıkan sonuçtaki `ADMIN_API_KEY=...` kısmındaki kodu kopyalayıp giriş ekranındaki şifre alanına yapıştırın.

Eğer `.env` dosyası yoksa veya key tanımlı değilse, geçici bir key oluşturup projeyi yeniden başlatabilirsiniz:

1.  `backend/.env` dosyasını açın (yoksa oluşturun).
2.  İçine şu satırı ekleyin: `ADMIN_API_KEY=GucluBirSifreBelirleyin123!`
3.  Projeyi yeniden başlatın: `docker compose up -d --build`